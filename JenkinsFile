pipeline {
    agent any

    stage('Checkout code') {
        steps {
            checkout scm
        }
    }

    stages {
        stage('Hello Stage') {
            steps {
                // This step prints 'Hello' to the console output
                echo 'Hello'
            }
        }
    }
}